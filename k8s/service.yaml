apiVersion: v1
kind: Service
metadata:
  name: esempioDevops-service
spec:
  selector:
    app: esempioDevops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30000
  type: NodePort